# Google Apps Script - フォーム自動応答システム

このディレクトリには、Googleフォームと連携したスプレッドシートで動作するGoogle Apps Script (GAS) が含まれています。

## ファイル構成

- **Code.gs**: メインのGoogle Apps Scriptコード

## 機能概要

### 機能①：フォーム送信時の自動返信メール
- **関数名**: `sendAutoReply`
- **動作タイミング**: フォーム送信時（トリガー）
- **機能詳細**:
  - フォーム送信者に自動返信メールを送信
  - お問い合わせ種別（資料請求/日程相談/その他）に応じてメール本文を変更
  - 重複送信を防止
  - 送信後、対応ステータスを「未対応」に設定

### 機能②：対応完了時の自動日時入力
- **関数名**: `onEditTrigger`
- **動作タイミング**: セル編集時（トリガー）
- **機能詳細**:
  - F列（対応ステータス）が「完了」に変更された時のみ動作
  - G列（対応完了日時）が空欄の場合のみ現在日時を自動入力
  - 手動入力された日時は上書きしない

## スプレッドシートの列構成

| 列 | 項目名 | 説明 |
|---|--------|------|
| A | タイムスタンプ | フォーム送信日時（自動） |
| B | 名前 | 送信者の名前 |
| C | メールアドレス | 送信者のメールアドレス |
| D | お問い合わせ種別 | 資料請求／日程相談／その他 |
| E | お問い合わせ内容 | 自由記述 |
| F | 対応ステータス | 未対応／対応中／完了 |
| G | 対応完了日時 | 自動入力（完了時のみ） |

## セットアップ手順

### 1. Google Apps Scriptエディタを開く
1. 対象のGoogleスプレッドシートを開く
2. メニューから「拡張機能」→「Apps Script」を選択
3. 新しいスクリプトエディタが開く

### 2. コードをコピー
1. `Code.gs` の内容を全てコピー
2. スクリプトエディタに貼り付け
3. 「保存」アイコンをクリック

### 3. トリガーを設定

#### sendAutoReply 関数用のトリガー（フォーム送信時）
1. スクリプトエディタで左側の「トリガー」（時計アイコン）をクリック
2. 右下の「トリガーを追加」をクリック
3. 以下のように設定:
   - **実行する関数**: `sendAutoReply`
   - **イベントのソース**: `スプレッドシートから`
   - **イベントの種類**: `フォーム送信時`
4. 「保存」をクリック
5. 初回は権限の承認が必要な場合があります

#### onEditTrigger 関数用のトリガー（編集時）
1. スクリプトエディタで左側の「トリガー」（時計アイコン）をクリック
2. 右下の「トリガーを追加」をクリック
3. 以下のように設定:
   - **実行する関数**: `onEditTrigger`
   - **イベントのソース**: `スプレッドシートから`
   - **イベントの種類**: `編集時`
4. 「保存」をクリック

### 4. 動作確認
1. Googleフォームからテスト送信を行う
2. メールが届くことを確認
3. スプレッドシートのF列を「完了」に変更
4. G列に日時が自動入力されることを確認

## 注意事項

- シート名は必ず「**フォームの回答 1**」にしてください
- メールアドレスが正しく入力されていることを確認してください
- 初回実行時はGoogleアカウントの権限承認が必要です
- トリガーの実行ログは「実行数」から確認できます

## トラブルシューティング

### メールが送信されない場合
1. トリガーが正しく設定されているか確認
2. メールアドレスが正しいか確認
3. スクリプトエディタの「実行数」でエラーログを確認

### 日時が自動入力されない場合
1. F列が「完了」に設定されているか確認（完全一致）
2. G列が空欄かどうか確認
3. トリガーが正しく設定されているか確認

### エラーログの確認方法
1. スクリプトエディタで「実行数」をクリック
2. エラーがある場合、詳細を確認できます

## セキュリティ

- このスクリプトは外部APIを使用しません
- MailApp（Gmail）のみを使用してメールを送信します
- スプレッドシートのデータは外部に送信されません

## ライセンス

このコードは自由に使用・改変できます。
